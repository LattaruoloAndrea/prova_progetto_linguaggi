proc nyFunction(x:int, y:real) : real {
  
  var newResult: real = 0.0;
  var counter: int = 0;
  var x:int = 0;

  while myBool {  
    var y:int = 0;
    counter+=1;
  }
  return fib(x); // error call function not seen? maybe
  }

    proc fib(n:int): int {
    if ( false ) {
        return 5;
    }else {
      writeInt(n);
      return 0;
    }  
  }