proc nyFunction(x:int, y:real) : real {
  
  var newResult: real = 0.0;
  var counter: int = 0;
  var x:int = 0;

  while myBool {  // error condition while not decleared
    var y:int = 0;
    counter+=1;
  }
  return fib(x); 
  }

    proc fib(n:int): int {
    if ( false ) {
        return 5;
    }else {
      writeInt(n);
      return 0;
    }  
  }
  
proc main(): void{
  return;
}